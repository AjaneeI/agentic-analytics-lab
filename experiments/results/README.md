# Experiment Results

This directory is reserved for reviewed benchmark outputs.

Raw JSON result files are ignored by default because early runs may contain
known-failed answers, stale latency measurements, or local-only debugging
context. Publish a result here only after it has been reviewed against the
current code and clearly labeled as a benchmark, failure case, or historical
baseline.
